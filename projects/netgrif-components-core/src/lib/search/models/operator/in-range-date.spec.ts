import {InRangeDate} from './in-range-date';
import {TestBed} from '@angular/core/testing';

describe('InRangeDate', () => {
    it('should create an instance', () => {
        expect(new InRangeDate()).toBeTruthy();
    });

    afterEach(() => {
        TestBed.resetTestingModule();
    });
});
