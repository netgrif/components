import {LessThanEqual} from './less-than-equal';
import {TestBed} from '@angular/core/testing';

describe('LessThanEqual', () => {
    it('should create an instance', () => {
        expect(new LessThanEqual()).toBeTruthy();
    });

    afterEach(() => {
        TestBed.resetTestingModule();
    });
});
