import {MoreThan} from './more-than';
import {TestBed} from '@angular/core/testing';

describe('MoreThan', () => {
    it('should create an instance', () => {
        expect(new MoreThan()).toBeTruthy();
    });

    afterEach(() => {
        TestBed.resetTestingModule();
    });
});
