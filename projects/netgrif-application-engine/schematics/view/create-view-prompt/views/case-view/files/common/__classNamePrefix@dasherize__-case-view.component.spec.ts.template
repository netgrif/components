import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { <%= classify(classNamePrefix) %>CaseViewComponent } from './<%= dasherize(classNamePrefix) %>-case-view.component';
import {
    CaseHeaderService,
    ConfigurationService,
    HeaderModule,
    MaterialModule,
    NAE_TAB_DATA
} from '@netgrif/application-engine';
import {CUSTOM_ELEMENTS_SCHEMA} from '@angular/core';
import {HttpClientTestingModule} from '@angular/common/http/testing';
import {<%= configName %>ConfigurationService} from '<%= configImportPath %>';
import {BrowserAnimationsModule} from '@angular/platform-browser/animations';

describe('<%= classify(classNamePrefix) %>CaseViewComponent', () => {
    let component: <%= classify(classNamePrefix) %>CaseViewComponent;
    let fixture: ComponentFixture<<%= classify(classNamePrefix) %>CaseViewComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            imports: [
                MaterialModule,
                HttpClientTestingModule,
                HeaderModule,
                BrowserAnimationsModule
            ],
            providers: [
                {provide: NAE_TAB_DATA, useValue: {tabViewComponent: undefined, tabViewOrder: 1}},
                {provide: ConfigurationService, useClass: <%= configName %>ConfigurationService},
                CaseHeaderService
                ],
            declarations: [<%= classify(classNamePrefix) %>CaseViewComponent],
            schemas: [CUSTOM_ELEMENTS_SCHEMA]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(<%= classify(classNamePrefix) %>CaseViewComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
