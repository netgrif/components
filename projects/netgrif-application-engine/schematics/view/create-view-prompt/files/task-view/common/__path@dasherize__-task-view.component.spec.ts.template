import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { <%= classify(path) %>TaskViewComponent } from './<%= dasherize(path) %>-task-view.component';
import {
    MaterialModule,
    HeaderModule,
    ConfigurationService,
    PanelModule,
    AuthenticationMethodService,
    NAE_TAB_DATA,
    SimpleFilter,
    FilterType
} from '@netgrif/application-engine';
import {BrowserAnimationsModule} from '@angular/platform-browser/animations';
import {<%= configName %>ConfigurationService} from '<%= configImportPath %>';

describe('<%= classify(path) %>TaskViewComponent', () => {
    let component: <%= classify(path) %>TaskViewComponent;
    let fixture: ComponentFixture<<%= classify(path) %>TaskViewComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            imports: [
                MaterialModule,
                HeaderModule,
                PanelModule,
                BrowserAnimationsModule
            ],
            providers: [
                {provide: NAE_TAB_DATA, useValue: {baseFilter: new SimpleFilter('id', FilterType.TASK, {})}},
                AuthenticationMethodService,
                {provide: ConfigurationService, useClass: <%= configName %>ConfigurationService}
            ],
            declarations: [<%= classify(path) %>TaskViewComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(<%= classify(path) %>TaskViewComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
