import { ParentWidthDependantStyleDirective } from './parent-width-dependant-style.directive';

describe('ParentWidthDependantStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ParentWidthDependantStyleDirective();
    expect(directive).toBeTruthy();
  });
});
