import {AbstractFilterFieldComponent} from './abstract-filter-field.component';

describe('AbstractFilterFieldComponent', () => {
    it('should create an instance', () => {
        expect(new AbstractFilterFieldComponent()).toBeTruthy();
    });
});
