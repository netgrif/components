import { NullTaskListOperations } from './null-task-list-operations';

describe('NullTaskListOperations', () => {
  it('should create an instance', () => {
    expect(new NullTaskListOperations()).toBeTruthy();
  });
});
