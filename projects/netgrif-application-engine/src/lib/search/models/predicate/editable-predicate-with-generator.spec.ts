import {EditablePredicateWithGenerator} from './editable-predicate-with-generator';

describe('EditablePredicateWithGenerator', () => {
    it('should create an instance', () => {
        expect(new EditablePredicateWithGenerator()).toBeTruthy();
    });
});
