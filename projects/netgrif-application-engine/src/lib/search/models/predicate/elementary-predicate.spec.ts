import {ElementaryPredicate} from './elementary-predicate';

describe('ElementaryPredicate', () => {
    it('should create an instance', () => {
        expect(new ElementaryPredicate()).toBeTruthy();
    });
});
