import {ConfigurationInput} from './configuration-input';

describe('ConfigurationInput', () => {
    it('should create an instance', () => {
        expect(new ConfigurationInput()).toBeTruthy();
    });
});
