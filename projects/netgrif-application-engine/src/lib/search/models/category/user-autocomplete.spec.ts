import {UserAutocomplete} from './user-autocomplete';

describe('UserAutocomplete', () => {
    it('should create an instance', () => {
        expect(new UserAutocomplete(undefined)).toBeTruthy();
    });
});
