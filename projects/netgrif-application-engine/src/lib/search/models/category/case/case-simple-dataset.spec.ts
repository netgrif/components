import { CaseSimpleDataset } from './case-simple-dataset';

describe('CaseSimpleDataset', () => {
  it('should create an instance', () => {
    expect(new CaseSimpleDataset()).toBeTruthy();
  });
});
