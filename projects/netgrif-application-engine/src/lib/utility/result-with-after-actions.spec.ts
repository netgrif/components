import { ResultWithAfterActions } from './result-with-after-actions';

describe('ResultWithAfterActions', () => {
  it('should create an instance', () => {
    expect(new ResultWithAfterActions()).toBeTruthy();
  });
});
