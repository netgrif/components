define(['angular', '../modules/Search', '../modules/Main'],
    function (angular) {
        angular.module('ngSearch').controller('SearchController',
            ['$log', '$scope', '$timeout',
                function ($log, $scope, $timeout) {
                    const self = this;

                }]);
    });
