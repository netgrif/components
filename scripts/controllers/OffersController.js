
define(['angular', '../modules/Offers', '../modules/Main'],
function (angular) {
   angular.module('ngOffers').controller('OffersController',
       ['$log', '$scope', '$timeout',
        function ($log, $scope, $timeout) {
            const self = this;

        }]);
});
